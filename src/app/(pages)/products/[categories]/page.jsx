import Categories from "@/app/components/categories/Categories";
import React from "react";

const page = ({ params }) => {
  return (
    <div>
      <Categories />
    </div>
  );
};

export default page;
